<footer role="contentinfo" class="footer">
	<section>
		<div class="footer-banner">
			<a href="{{base_url}}"><img class="footer-banner-logo" src="{{base_url}}assets/img/logo-flvm-compact-white.png" alt="Faites le vous même"></a>
			<div class="footer-banner-description">
				<h2>{{tanuki.title}}</h2>
				<p>
					{{{tanuki.description}}}
				<p>
			</div>
		</div>
	</section>
	<section>

		L'association Faites-Le Vous-Même vous propose des cours de cuisine
		et de pâtisserie à Rouen. Retrouvez-vous pour partager un moment
		convivial et gourmand.

	</section>
	<section class="sub-footer">
		<p>
			<a href="{{tanuki.publisher.url}}">{{tanuki.publisher.name}}</a>, <a href="{{tanuki.publisher.email}}">{{tanuki.publisher.email}}</a> — Vous pouvez participer librement au contenu (corrections, ajouts, etc.) via <a href="https://github.com/FLVM/FLVM-website-content">le dépot partagé</a>.
		</p>
		<p>
			Le code source est sous licence <a href="{{tanuki.license.source..url}}">{{tanuki.license.source.name}}</a> {{current_year}}
			—
			Le contenu est sous licence <a href="{{tanuki.license.content.url}}">{{tanuki.license.content.name}}</a> {{current_year}}
		</p>
	</section>
</footer>